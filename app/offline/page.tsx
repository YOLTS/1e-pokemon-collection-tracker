import { OfflineModeClient } from "@/components/OfflineModeClient";

export const metadata = {
  title: "Offline Card Show Mode",
};

export default function OfflinePage() {
  return <OfflineModeClient />;
}
